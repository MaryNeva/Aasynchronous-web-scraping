Сервер, с которого вы будете собирать данные, не очень мощный и рассчитан только на 75 запросов от клиентов в секунду. По этой причине, вам необходимо использовать семафор для ограничения количества одновременных запросов, чтобы избежать постоянных разрывов.

Ссылка (https://asyncio.ru/zadachi/2/problem_pages.txt) содержит список проблемных страниц, каждая из которых указана на новой строке. Эти строки представляют собой имена страниц, которые нужно будет использовать для формирования полных URL-адресов.

Для каждого числа, необходимо сформировать полный URL-адрес, добавив к нему базовый URL и расширение .html. Если имя страницы в файле — 10275454, то полный URL будет: https://asyncio.ru/zadachi/2/html/10275454.html

Каждая страница содержит тег с id='number', в котором находится число. Это число нужно извлечь и добавить к общей сумме. Ваша задача суммировать все числа со всех страниц находящихся в файле. Для извлечения данных из тега используйте библиотеку BeautifulSoup.
